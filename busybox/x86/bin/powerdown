#!/bin/sh
echo -n "prepare for cleaning persistance shutdown   .." | tr -s "/" >&2
   for timeout in {1..10} ; do
      echo -n "." >&2
      sleep 1
      
   done
   echo "" >&2
    
 exec /mnt/live/lib/cleanup --start poweroff